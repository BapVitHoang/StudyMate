# DEPRECATED for the Android app AI path.
# StudyMate now calls Gemini directly from the device using GEMINI_API_KEY in
# project-root local.properties (see local.properties.example).
#
# This file is only for optional local Firebase Functions emulator experiments.
# Cloud Functions + secrets require Firebase Blaze and are NOT required anymore.
#
# GEMINI_API_KEY=your_gemini_api_key_here
